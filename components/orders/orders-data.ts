/**
 * Mock order fixtures removed — Orders is wired to the real API via
 * lib/api/commerce.ts (listOrders / getOrder / updateOrderStatus).
 * Status values are the backend's five states (pending|paid|fulfilled|
 * cancelled|refunded); there is no structured payment-method field.
 */
